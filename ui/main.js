import React from 'react';
import ReactDOM from 'react-dom';

const Container = props => {
  return <div>Open for business!!!</div>;
}

ReactDOM.render(<Container />, document.getElementById("app"));
